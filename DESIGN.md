# HMTI 디자인 시스템 (Mobbin 패턴 기반)

스택: Next.js 15 App Router · Emotion(styled) · GSAP · Material Symbols Rounded · Pretendard JP Variable · KO/EN/JA

## 브랜드 컬러 (2026-09-14 추가)
- `brand #C8385A` 진홍: 보조금 금액, 관심 하트, 활성 탭, 로고마크, 진행바, 인증 배지
- `brandSoft #FCE9EE` 파스텔 워시: 보조금 카드 배경, 주민 아바타
- `brandTint #F5C3CF` 보조. 기존 `accent`는 brand와 동일값으로 통일.

## 레퍼런스: 여기어때 (2026-09-14)
- 페이지 배경은 순백 #ffffff (회색 바탕은 카드가 떠 보여 폐기). 섹션은 헤어라인으로 구분, 검색 카드는 헤어라인+연한 그림자로 형태를 잡는다. 카드·버튼 라운드 10~12px, 칩만 필.
- 사진 위 흰 텍스트: 이미지 brightness .78~.82 + 스크림(배너는 좌→우 .66→.14, 타일은 하단 45%→.6). 텍스트 섀도만으로는 부족.
- 간격 3단계: 요소 안 4~8px, 요소 사이 14~16px, 섹션 안쪽 28~32px. 캐러셀 gap 14px. 가격 숫자는 tabular-nums, 사진은 1px 중립 아웃라인.
- 상단: 브랜드색 워드마크, 카테고리 원형 아이콘(활성 = 브랜드 링), 흰 검색 카드 + 브랜드색 솔리드 버튼 48px/700.
- 섹션: 제목 18~20/700 + "더보기 >", 가로 캐러셀, 점 인디케이터, 지역 사진 타일.
- 매물 카드: 정가 취소선 → 보조금 % (브랜드색) → 적용가 굵게. 할인율 문법을 보조금으로 치환.
- 다크 "Pick" 섹션(큐레이션), FAQ 아코디언, 고객센터 푸터.

## AI 리모델링 (`lib/types.ts`, `app/api/renovate`)
- 유형 16개 = 축 4개 × 극 2개. 코드 글자별 프롬프트 조각(H/R, C/S, B/M, L/I)을 이어 붙여 스타일 지시문을 만든다. 공통부: 같은 앵글·형태·창 위치 유지, 손상 복구, 따뜻한 저녁빛, 텍스트·사람 금지.
- 모델: gemini-3.1-flash-lite-image 우선(약 ₩46/장, 5~6초), 없으면 flash-image → 2.5 순 폴백. 키는 서버 환경변수만.
- 클라이언트는 1280px JPEG로 축소 후 전송(4.5MB 본문 제한). 실패 시 CSS 필터 예시 + 안내 문구.
- 진단 결과에서 유형 코드를 localStorage `hmti_type`에 저장, 촬영 시 함께 전송.

## 진단 (16personalities 문법)
- 문항 16개, 축 4개(일·관계·집·예산) × 4문항, 홀수 문항은 역채점. 답은 -3~3, 축 점수 = (합+12)/24.
- 척도 원: 44/36/28/22/28/36/44, 동의 쪽 브랜드, 비동의 쪽 잉크, 중앙 회색. 히트 영역 44px.
- 코드 = 축별 우세 극 글자(H/R, C/S, B/M, L/I). 유형 매핑은 `score()`: R 우세→원격러, C&I→호스트, 나머지→정착러.
- 스크롤바는 전역 숨김(html/body).

## 자간·행간 (Pretendard)
- 본문 15px/1.5, 자간 -0.01em. 헤딩 -0.02em(24px) ~ -0.03em(56px). 12px 라벨·캡션은 자간 0.
- 카드 박스는 홈 검색 카드 하나만. 나머지 정보 블록은 헤어라인 행/그리드(padding 14~16px 0)로.
- 탭바: 기본은 아이콘 200 웨이트·라벨 400·muted, 선택만 채움 500·700·브랜드색.
- 라벨은 박스 없이 텍스트만(카드 특징 태그, 커뮤니티 카테고리). 솔리드 태그는 즉시 입주 하나뿐.
- 캐러셀 점은 `Carousel` 컴포넌트가 실제 scrollLeft로 계산(가장 가까운 카드 왼쪽 끝, 끝에서는 마지막). 탭하면 해당 카드로 이동.
- `[data-reveal]`는 CSS로 숨기지 않고 GSAP fromTo로만 처리(마운트 후 추가된 요소가 영영 안 보이는 사고 방지).

## 한국어 카피 규칙 (humanize-korean 2026-09-15 검토 반영)
- "·" 나열 금지(출처·크레딧 줄 제외). 쉼표나 조사로 풀어 쓴다.
- 헤드라인 끝 마침표 금지. 명사구로 끝낸다.
- 2인칭 "당신" 금지 → "나의" 또는 생략. 설명문은 "~해요/~드려요" 구어체 존댓말.
- "~이 맞습니다", 명사 종결, 불필요한 피동 같은 직역투 금지. 조사로 끊긴 미완성 문장 금지.
- 수치·화폐·브랜드·기관명은 그대로.

## 통화 (`lib/money.ts`)
- 매물 가격·월 예상·보조금은 숫자 + 원통화(JPY/KRW)로 저장. 표시 시 로케일 통화로 환산: ko → ₩, en → $, ja → ¥.
- 고정 환율(1 JPY = 9.2 KRW, 1 USD = 1,380 KRW), 반올림: KRW 만원/천원 단위, JPY 만엔/천엔, USD 100/10달러. 예산 셀은 ko에서 "3,200만원" 단축 표기.
- 지원 제도·이벤트 문구 속 금액도 로케일 통화로 적는다(ko 920만원, en $6,700, ja 100万円).

## 금지 패턴 (AI 티)
- 회색 카드 + 아이콘 타일 + 제목 + 설명 조합. 프로모 배너도 마찬가지: 회색 박스에 원형 아이콘 금지, 사진 위 스크림 + 카피로. 기능은 아이콘이 아니라 실제 화면(비포/애프터 분할 사진, 첫 질문 티저)으로 보여준다.
- 떠 있는 회색 필 헤더, 탭바 블롭, 거대한 검은 푸터, 아이콘 마퀴.
- 파스텔 원 + 링 + 아이콘 카테고리 금지. 퀵메뉴는 흰 바탕에 3D 아이콘(3dicons CC0, `public/3d/*.png`, 48px, 옅은 drop-shadow) + 라벨. 바로가기에 "활성" 상태를 주지 않는다.
- 이니셜 아바타 금지. 커뮤니티 아바타는 원형 인물 사진(36px, 1px 중립 아웃라인), 사진이 없을 때만 이니셜 폴백.
- 파스텔 배경 배지 금지. 태그는 정보가 있을 때만: 즉시 입주(잉크 솔리드) / 특징 1개(헤어라인 아웃라인, 11px). 모든 카드에 붙는 태그는 노이즈라 제거.
- 메뉴 행은 타일 없이 아이콘(400) + 텍스트 + 헤어라인.

## 원칙
- **모노크롬 갤러리**: 잉크 #141414 on 흰 캔버스. 사진만 채도를 가진다. 이모지 금지, 전부 Material Symbols.
- **필(pill) 인터랙션**: 버튼·배지·칩은 `radius.full`. 헤더와 탭바는 흰 바탕 + 헤어라인(상용 앱 문법), 상세는 사진 위 투명 헤더 + 하단 고정 CTA.
- **그림자 없는 깊이**: 틴트 사다리(canvas → canvas-soft → field → hairline)와 1px 헤어라인으로만 위계. 사이드 스트라이프 카드 금지, 그라데이션은 마퀴 페이드 마스크 한 곳뿐.
- **액센트 하나**: #0066ff는 보조금·절약 같은 상업 신호에만. CTA에는 절대 쓰지 않음.
- **타이포 대비**: 650 헤딩(행간 1.0~1.13) + 300 라이트 리드. 자간 0, 문장형 + 마침표.
- **페이지 세분화**: 한 화면 한 목적. 상세는 개요/지역/지원 3페이지, 진단·시뮬은 인트로/진행/결과 3페이지.
- **모션**: 라우트 전환마다 페이지 전체가 0.38s 페이드업(`app/template.tsx` → `Reveal`), `[data-reveal]` 요소는 이어서 stagger. prefers-reduced-motion 시 즉시 표시. 캐러셀은 `scroll-padding-inline: 20px`로 스냅 시 거터 유지.

## 토큰 (`lib/theme.ts`)
| 토큰 | 값 | 용도 |
|---|---|---|
| ink / inkSoft | #141414 / #262626 | 텍스트, 주 CTA, 푸터, 활성 탭 |
| canvas / canvasSoft | #fff / #f3f3f3 | 페이지·카드 / 내비 필, 강조 카드, 세그먼트 트랙 |
| field | #f0f0f0 | 입력 필드(테두리 없음, 포커스 시 2px 잉크 링) |
| hairlineSoft / hairline | #f0f0f0 / #e0e0e0 | 카드 외곽선 / 아웃라인 버튼 |
| muted / faint | #707070 / #adadad | 보조 텍스트 / 플레이스홀더 |
| accent | #0066ff | 상업 신호 전용 |
| overlay | rgba(115,115,115,.56) | 사진 위 배지 |
| radius sm / md / full | 16 / 24 / 9999 | 입력·행 / 카드·미디어 / 필 |
| squircle | border-radius 30% | 아이콘 타일 |

## 타이포 (Pretendard JP Variable)
| 컴포넌트 | 크기 | 웨이트 | 행간 |
|---|---|---|---|
| Display | 56 | 650 | 1.0 |
| H1 / H2 / H3 / H4 | 40 / 28 / 22 / 18 | 650·600 | 1.02~1.25 |
| Lead | 18 | 300 | 1.38 |
| Body / Small | 16 / 14 | 450 | 1.38 / 1.43 |
| Label / Caption | 12 | 600 / 450 | 1.33 |

## 컴포넌트 (`components/ui.tsx`)
Icon · Pill(primary/outline/soft, icon/trailing) · IconBtn · Card/CardLink(soft) · Chip · Badge · Squircle · ListRow · SegTrack/SegItem · Input/Textarea · TopBar · Media/Overlay · Stat · Step · Footer.
커스텀 prop은 `shouldForwardProp`으로 DOM/Link에 전달되지 않게 차단.

## 화면 맵
| 경로 | 내용 |
|---|---|
| / | 검색창, 지역 칩, 사진 히어로, 추천 캐러셀, 보조금 큰 집, 비포/애프터 데모 + 퀴즈 티저, 이주민 이야기 |
| /about | 통계 카운트업, 문제 정의, TV 사례 2 |
| /houses · /map | 검색+칩 필터 목록 · Leaflet + OSM 타일(드래그·핀치줌), 뷰마다 48px 그리디 클러스터, 클러스터 탭 시 2단계 줌인, 핀 탭 시 아래 카드 갱신 |
| /houses/[id] · /area · /support | 개요 · 일자리/교통 · 지원 제도 |
| /diagnosis · /quiz · /result?a= | 인트로 · 16문항 7단계 리커트(세로 나열, 답하면 흐려지고 자동 스크롤) · 4축 % 바 + 4글자 코드 + 예산 + 추천 |
| /simulate · /upload?house= · /result | 인트로 · 실제 카메라 뷰파인더(getUserMedia, 후면 기본, 전환·앨범·셔터, 촬영 후 다시 찍기/분석) · 비포/애프터 슬라이더+지표 |
| /community · /new · /[id] | 원형 인물 사진 아바타, 주민 배지, 카테고리(질문·소식·후기) 탭 필터, 글 사진 첨부, 헤어라인 행 피드 · 카테고리 선택+작성 · 상세+답글(아바타) |
| /settings | 언어 선택(localStorage) |
